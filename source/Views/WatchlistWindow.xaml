<Window x:Class="Sieve.Views.WatchlistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Sieve.ViewModels"
        mc:Ignorable="d"
        Title="Watchlist Data Entry" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Window.DataContext>
        <vm:WatchlistWindowViewModel />
    </Window.DataContext>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Natural Language Watchlist Entry" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Input Section -->
        <GroupBox Grid.Row="1" Header="Enter Items (Natural Language)" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBox Grid.Row="0" 
                         Text="{Binding NaturalLanguageInput, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         Padding="5"
                         Margin="0,0,0,10">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Modifiers="Ctrl" Command="{Binding ParseAndAddCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Parse and Add" 
                            Command="{Binding ParseAndAddCommand}"
                            Padding="15,5" 
                            Margin="0,0,5,0"
                            FontSize="14">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="IsEnabled" Value="True"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Save" 
                            Command="{Binding SaveCommand}"
                            Padding="15,5"
                            Margin="0,0,5,0"
                            FontSize="14"/>
                    <Button Content="Clear All" 
                            Command="{Binding ClearAllCommand}"
                            Padding="15,5"
                            FontSize="14"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Status Message -->
        <TextBlock Grid.Row="2" 
                   Text="{Binding StatusMessage}" 
                   FontStyle="Italic" 
                   Foreground="Gray"
                   Margin="0,0,0,10"/>

        <!-- Watchlist Items -->
        <GroupBox Grid.Row="3" Header="Watchlist Items" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="All"
                      HeadersVisibility="Column"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="Element Type" Binding="{Binding ElementType}" Width="120"/>
                    <DataGridTextColumn Header="Parameter" Binding="{Binding ParameterName}" Width="120"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding ParameterValue}" Width="120"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    <DataGridTemplateColumn Header="Actions" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remove" 
                                        Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Padding="5,2"
                                        FontSize="11"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Footer -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Tip: Use natural language like 'Watch walls with Height &gt; 10' or 'Monitor doors in category Doors'" 
                       FontSize="11" 
                       Foreground="DarkGray"
                       TextWrapping="Wrap"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center"/>
            <Button Content="Close" 
                    Click="CloseButton_Click"
                    Padding="15,5"
                    FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>

